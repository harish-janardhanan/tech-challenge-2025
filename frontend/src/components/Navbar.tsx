import React from 'react';

const Navbar = () => (
    <nav className="bg-white w-full shadow-lg  mx-auto  px-6 py-3 flex items-center">
        <div className="font-bold text-lg">Trade Platform</div>
        <div className="space-x-4">
            {/* Add navigation links here */}
        </div>
    </nav>
);

export default Navbar;
